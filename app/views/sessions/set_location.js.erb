$('p.coordinates').html("<%= escape_javascript(session[:coordinates].to_s) %>");
$('p.debug').html("<%= escape_javascript(debug session) %>");

$('input#address').val("<%= session[:coordinates] %>");

//relace table results
$('.services-body').html("<%= escape_javascript(render(@services)) %>");

//replace pagination data
$('.pagination').html('<%= escape_javascript(paginate(@services, :remote => true).to_s) %>');
